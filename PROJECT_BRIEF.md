# Project Brief — Show What You Know: Provenance Guard

> Reference document. Captures the full project spec so both Linnea and Claude can
> check work against requirements as we go. This is **not** a deliverable —
> `planning.md` and `README.md` are the graded deliverables.

**Estimated effort:** ~9–11 hours total

---

## The Problem

Platforms where people share original creative work — writing, music, art — face a new
challenge: how do you know whether what someone posted was made by them, or generated by
AI and passed off as human? Not to police creativity, but to **protect attribution, build
trust, and give audiences the context they actually need**.

This project builds **Provenance Guard**: a backend system that any creative sharing
platform could plug into to:

1. Classify submitted content
2. Score confidence in that classification
3. Surface a transparency label to users
4. Handle appeals from creators who believe they've been misclassified

This is the most independent project so far. **No planning template is provided** — the
system architecture is designed from scratch before writing implementation code.

---

## 📋 Deliverables & Format (easy-to-miss rules)

- **Transparency label → written as text in the README.** The README must include the
  **verbatim text of all three label variants** (high-confidence AI, high-confidence
  human, uncertain) as a quoted string or a markdown table. A screenshot/mockup is a
  welcome extra, but **the written text is what's required — a screenshot alone is not
  enough.**
- **Architecture diagram → in `planning.md`** under an `## Architecture` section. ASCII
  art is fine (so is a committed Mermaid block or an embedded sketch).
- **No starter repo, no planning template.** Create your own GitHub repo and design the
  `planning.md` structure yourself.
- **Canonical record graders rely on:** keep all evidence — audit-log sample, rate-limit
  configuration and chosen limits, label variants, and appeal handling — in the
  **README and committed source code**.

---

## 🎯 Goals

By completing this project, you will be able to:

- Design and implement a multi-signal AI content classification pipeline.
- Build confidence scoring that communicates uncertainty rather than forcing binary outputs.
- Create end-user transparency labels that surface AI verdicts clearly and fairly.
- Implement an appeals workflow for contested classifications.
- Add production safety infrastructure: rate limiting and structured audit logging.

---

## ✅ Required Features

### 1. Content Submission Endpoint
- API endpoint that accepts a piece of text-based content (poem, short story excerpt,
  blog post) for attribution analysis.
- Must return a **structured response** including:
  - the attribution result
  - the confidence score
  - the transparency label text that would be shown to the user

### 2. Multi-Signal Detection Pipeline
- Must use **at least 2 distinct signals** to classify content. Single-signal detection
  is **not acceptable**.
- `planning.md` and `README` must explain **what each signal captures and why** it was chosen.

### 3. Confidence Scoring with Uncertainty
- Must return a **confidence score**, not just a binary label.
- The score must reflect genuine uncertainty — a **0.51 confidence should produce a
  meaningfully different transparency label than a 0.95**.
- README must explain the approach **and how you tested whether the scores are meaningful**.

### 4. Transparency Label
- Design and implement the label displayed to a reader on the platform.
- Must communicate the attribution result **in plain language** and make the confidence
  level meaningful to a **non-technical reader**.
- README must include a **typed description of all three label variants** (high-confidence
  AI, high-confidence human, uncertain) — the **exact text** each one displays.
- Screenshot/mockup welcome as an extra, but the written description is required.

### 5. Appeals Workflow
- Mechanism for creators to contest a classification. At minimum, an appeal must:
  - capture the creator's reasoning
  - log the appeal alongside the original decision
  - update the content's status to **"under review"**
- Automated re-classification is **not** required.

### 6. Rate Limiting
- Implement rate limiting on the submission endpoint.
- README must document **the limits chosen and the reasoning** for those specific values.

### 7. Audit Log
- **Every** attribution decision — including confidence score, signals used, and any
  appeals — must be captured in a **structured audit log**.
- Document the log in the README (or via `GET /log` output) with **at least 3 entries visible**.

---

## 🌟 Stretch Features (extra credit)

> Update `planning.md` before starting each one, and document any completed feature in the
> README (what you built and how it works), not just `planning.md`.

- **Ensemble detection:** 3+ detection signals with a documented weighting or voting approach.
- **Provenance certificate:** a "verified human" credential a creator earns through an extra
  verification step, including how it's displayed on their content.
- **Analytics dashboard:** a simple view showing detection patterns, appeal rates, and one
  additional metric of your choosing.
- **Multi-modal support:** extend the pipeline to handle a second content type (e.g., image
  descriptions or structured metadata) in addition to text.

---

## 💡 Hints

- A **false positive** (labeling a human's work as AI-generated) is **worse** than a false
  negative on a writing platform. Does confidence scoring and label design reflect that
  asymmetry anywhere?
- The confidence score is a **design decision before it's a technical one**. Decide what
  0.5 should mean to a user, then figure out how to achieve that — not the other way around.
- The transparency label is a **UX problem** as much as a technical one. Show the label to
  someone who hasn't seen the project and ask what they understand from it before finalizing.
- Rate-limiting numbers aren't arbitrary. Think about realistic usage on a writing platform:
  how often does a single creator submit work? How would an adversary try to flood the
  system? Document the reasoning.
- Detection doesn't need to be perfect — perfect AI detection is an unsolved problem. The
  system should **acknowledge uncertainty honestly** and give creators a path to appeal.
  That's the real engineering challenge.
- Before reaching for AI assistance, go back to the spec. If a feature is unclear, the
  answer is usually in a question not thought through during planning.

---

## 🛠️ Recommended Stack (free, no new accounts beyond existing Groq)

| Component          | Tool                              | Notes                                   |
|--------------------|-----------------------------------|-----------------------------------------|
| API framework      | Flask                             | Free, lightweight                       |
| Detection signal 1 | Groq (`llama-3.3-70b-versatile`)  | Free tier — same account as Projects 1–3 |
| Detection signal 2 | Stylometric heuristics            | Pure Python, no external libraries      |
| Rate limiting      | Flask-Limiter                     | Free                                    |
| Audit log          | SQLite (built-in) or structured JSON | No additional setup                  |

---

## Requirements Checklist (track as we build)

**Deliverables / format**
- [ ] GitHub repo created (own structure)
- [ ] `planning.md` with `## Architecture` section + diagram
- [ ] `README.md` with all three label variants written out verbatim
- [ ] Audit-log sample in README/source
- [ ] Rate-limit config + chosen limits documented in README
- [ ] Appeal handling documented in README

**Required features**
- [ ] 1. Content submission endpoint (returns result + confidence + label text)
- [ ] 2. Multi-signal detection pipeline (≥2 signals, explained)
- [ ] 3. Confidence scoring with uncertainty (0.51 ≠ 0.95 label; tested)
- [ ] 4. Transparency label (3 variants, plain language, non-technical)
- [ ] 5. Appeals workflow (reasoning + log + status "under review")
- [ ] 6. Rate limiting (documented limits + reasoning)
- [ ] 7. Audit log (structured, ≥3 entries visible)

**Stretch (optional)**
- [ ] Ensemble detection (3+ signals, weighting/voting)
- [ ] Provenance certificate ("verified human")
- [ ] Analytics dashboard
- [ ] Multi-modal support
